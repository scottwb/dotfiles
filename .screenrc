# Remap the normal C-a screen escape key to be C-p instead, so that the
# C-a we all know in love in bash, emacs, etc. can continue to be useful.
escape ^pp # Instead of CTRL-a, make the escape/command char be CTRL-p
vbell off
autodetach on  # Autodetach session on hangup instead of terminating screen
starup_message off  # Turn off the spash screen
defscrollback 30000 # Use a 30000-line scrollback buffer
