Deprecated alias for /gameplan (renamed so it no longer collides with Claude Code's built-in plan mode).

Arguments: $ARGUMENTS (passed through unchanged)

This command was renamed to `/gameplan`. Behave exactly as if the user had run `/gameplan $ARGUMENTS`: read `~/.claude/commands/gameplan.md` and follow it verbatim.

Before starting, tell the user once, briefly: "(FYI: /plan is now /gameplan; this alias still works.)" Then proceed. Do not nag about it again in the same session.
